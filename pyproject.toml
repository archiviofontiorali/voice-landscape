[project]
name = "voice-landscape"
version = "0.9.0"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=5.2",
]

[dependency-groups]
lab = [
    "marimo>=0.23.6",
    "polars>=1.40.1",
]
prod = [
    "gunicorn>=26.0.0",
]
