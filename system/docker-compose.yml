# Use postgres/example user/password credentials
version: '3.1'

services:

  db:
    image: postgis/postgis
    restart: always
    environment:
      POSTGRES_PASSWORD: lv-password
    ports:
      - "54320:5432"

  adminer:
    image: adminer
    restart: always
    ports:
      - "8080:8080"